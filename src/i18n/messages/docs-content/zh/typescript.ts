import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "类型",
  "intro": "intent-link 拥有完整的类型定义。直接导入这些类型即可。",
  "typesHeading": "类型",
  "types.props": "`IntentLinkProps` —— 扩展自 next/link 的 `LinkProps` 和锚点属性，并加上 `importance`、`cost`、`onIntent`。",
  "types.physics": "`PhysicsState` —— 单个目标的实时状态。",
  "types.context": "`IntentContextType` —— `IntentContext` 的结构。",
};

export { typescript };
