import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "타입",
  "intro": "intent-link는 완전히 타입이 지정되어 있습니다. 타입을 직접 임포트하세요.",
  "typesHeading": "타입",
  "types.props": "`IntentLinkProps` — next/link의 `LinkProps`와 앵커 속성을 확장하고, 여기에 `importance`, `cost`, `onIntent`를 더합니다.",
  "types.physics": "`PhysicsState` — 한 대상의 실시간 상태.",
  "types.context": "`IntentContextType` — `IntentContext`의 형태.",
};

export { typescript };
