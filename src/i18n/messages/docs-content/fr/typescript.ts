import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Types",
  "intro": "intent-link est entièrement typé. Importez les types directement.",
  "typesHeading": "Types",
  "types.props": "`IntentLinkProps` — étend les `LinkProps` de next/link et les attributs d'ancre, plus `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` — l'état en direct d'une cible.",
  "types.context": "`IntentContextType` — la forme d'`IntentContext`.",
};

export { typescript };
