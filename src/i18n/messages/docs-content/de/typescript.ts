import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Typen",
  "intro": "intent-link ist vollständig typisiert. Importiere die Typen direkt.",
  "typesHeading": "Typen",
  "types.props": "`IntentLinkProps` — erweitert die `LinkProps` von next/link und die Anker-Attribute, plus `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` — der Live-Zustand eines Ziels.",
  "types.context": "`IntentContextType` — die Form von `IntentContext`.",
};

export { typescript };
