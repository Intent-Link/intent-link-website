import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Tipos",
  "intro": "O intent-link é totalmente tipado. Importe os tipos diretamente.",
  "typesHeading": "Tipos",
  "types.props": "`IntentLinkProps` — estende o `LinkProps` do next/link e os atributos da âncora, mais `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` — o estado ao vivo de um alvo.",
  "types.context": "`IntentContextType` — o formato do `IntentContext`.",
};

export { typescript };
