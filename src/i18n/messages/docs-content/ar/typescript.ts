import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "الأنواع",
  "intro": "intent-link مُنمَّط بالكامل. استورد الأنواع مباشرة.",
  "typesHeading": "الأنواع",
  "types.props": "`IntentLinkProps` — يوسّع `LinkProps` الخاص بـ next/link وسمات الرابط، إضافة إلى `importance`، `cost`، `onIntent`.",
  "types.physics": "`PhysicsState` — الحالة الحية لهدف واحد.",
  "types.context": "`IntentContextType` — شكل `IntentContext`.",
};

export { typescript };
