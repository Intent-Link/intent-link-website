import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "型",
  "intro": "intent-link は完全に型付けされています。型を直接インポートしてください。",
  "typesHeading": "型",
  "types.props": "`IntentLinkProps` — next/link の `LinkProps` とアンカー属性を拡張し、さらに `importance`、`cost`、`onIntent` を加えたもの。",
  "types.physics": "`PhysicsState` — 1つの遷移先のリアルタイムの状態。",
  "types.context": "`IntentContextType` — `IntentContext` の形。",
};

export { typescript };
