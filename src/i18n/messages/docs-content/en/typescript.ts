import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Types",
  "intro": "intent-link is fully typed. Import the types directly.",
  "typesHeading": "Types",
  "types.props": "`IntentLinkProps` extends next/link's `LinkProps` and anchor attributes, plus `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` is one target's live state.",
  "types.context": "`IntentContextType` is the shape of `IntentContext`.",
};

export { typescript };
