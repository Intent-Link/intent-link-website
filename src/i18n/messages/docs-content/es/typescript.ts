import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Tipos",
  "intro": "intent-link está completamente tipado. Importa los tipos directamente.",
  "typesHeading": "Tipos",
  "types.props": "`IntentLinkProps` — extiende `LinkProps` de next/link y los atributos del anchor, más `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` — el estado en vivo de un objetivo.",
  "types.context": "`IntentContextType` — la forma de `IntentContext`.",
};

export { typescript };
