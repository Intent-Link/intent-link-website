import type { DocText } from "@/content/docs/types";

const typescript: DocText = {
  "toc.types": "Типы",
  "intro": "intent-link полностью типизирован. Импортируйте типы напрямую.",
  "typesHeading": "Типы",
  "types.props": "`IntentLinkProps` — расширяет `LinkProps` из next/link и атрибуты якоря, плюс `importance`, `cost`, `onIntent`.",
  "types.physics": "`PhysicsState` — живое состояние одной цели.",
  "types.context": "`IntentContextType` — форма `IntentContext`.",
};

export { typescript };
