/** Shared prop contract for every tile-category icon. Size and color are
 * controlled by the parent via `className` (icons render in `currentColor`). */
interface TileIconProps {
  className?: string;
}

export type { TileIconProps };
